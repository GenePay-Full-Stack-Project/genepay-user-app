name: genepay_user_app
description: "Genepay User Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.3
  http: ^1.2.1
  shared_preferences: ^2.2.3
  json_annotation: ^4.9.0
  mobile_scanner: ^5.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.9


flutter:

  
  uses-material-design: true

  assets:
    - assets/
